# Certificate scans

Referenced from `companyInfo.credentials` and shown in the home-page carousel.

| file | what it is |
|---|---|
| `quality-policy.jpg`  | Quality Policy, signed by the Director, 13-11-2020 |
| `iso-9001-2015.jpg`   | Otabu ISO 9001:2015, cert. 1127Q96720 |

Both were taken from the company's existing WordPress site at
platinaaceramics.com (`/wp-content/uploads/2020/12/`), which is where the
originals were already published. The Quality Policy scan was 1884x2560 / 1 MB
there; it is resized to 1200px wide here, which is more than the carousel can
display and about half the weight.

Replace either file in place to update — no code change needed. If you switch
format, update the path in `src/data/companyInfo.js`.
